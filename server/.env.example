PORT=3000
DB_PATH=./data/cloudpilot.db
# Generate with: openssl rand -base64 32
CONNECTION_ENCRYPTION_KEY=
# Optional. Used by the Anthropic SDK to generate plain-language explanations
# for findings. Without it, explanation generation silently stays unset
# (findings.explanation remains NULL); scanning is unaffected.
# ANTHROPIC_API_KEY=
